rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Only allow access to files under owners/{ownerId}/ if authenticated as that owner
    match /owners/{ownerId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == ownerId;
    }
  }
}
